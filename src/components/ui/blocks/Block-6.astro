---
import Carousel from '@/components/Carousel.astro'
import type { Block6 } from '@/components/ui/blocks/block-types'

type Props = Block6

const { title, description, medias } = Astro.props
---

<div class="grid grid-cols-12 w-full gap-indent" data-reveal-in-view>
  <div
    class="flex flex-col justify-between gap-3 col-span-12 md:col-span-5 md:pb-10"
  >
    <p class="text-headline-3">{title}</p>
    <p class="text-body">{description}</p>
  </div>
  <div class="col-span-12 md:col-span-7">
    <Carousel medias={medias} aspectRatio="square" />
  </div>
</div>
