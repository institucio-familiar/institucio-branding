---
import MediaAsset from '@/components/MediaAsset.astro'
import type { Block4 } from '@/components/ui/blocks/block-types'

type Props = Block4

const { title, description, media } = Astro.props
---

<section class="grid w-full gap-indent text-navy md:grid-cols-12">
  <div class="flex flex-col justify-end gap-3 col-span-5">
    <p class="text-body-bold">{title}</p>
    <p class="text-body">{description}</p>
  </div>
  <div class="aspect-4/3 w-full overflow-hidden rounded-sm col-span-7">
    <MediaAsset media={media} />
  </div>
</section>
