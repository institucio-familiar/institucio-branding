---
import Layout from '../layouts/Layout.astro'
import testSvg from '../assets/svgs/test.svg'
---

<!-- <Layout>
  <main class="flex min-h-screen flex-col items-end justify-center bg-ink">
    <div class="relative h-screen">
      <img
        src={testSvg.src}
        alt="Test SVG"
        width={testSvg.width}
        height={testSvg.height}
        class="absolute top-[50%] -translate-y-1/2 left-[-30px]"
      />
      <div class="bg-blue h-full w-[350px]"></div>
    </div>
  </main>
</Layout> -->
<Layout>
  <section class="flex min-h-screen items-center justify-center bg-mist">
    <h1 class="text-white text-headline-2">Test page</h1>
  </section>

  <section class="flex min-h-screen items-center justify-center bg-navy">
    <p class="text-white text-headline-3">Section 2 — Navy</p>
  </section>

  <section class="flex min-h-screen items-center justify-center bg-blue">
    <p class="text-white text-headline-3">Section 3 — Blue</p>
  </section>

  <section class="flex min-h-screen items-center justify-center bg-sky">
    <p class="text-white text-headline-3">Section 4 — Sky</p>
  </section>

  <section class="flex min-h-screen items-center justify-center bg-mist">
    <p class="text-ink text-headline-3">Section 5 — Mist</p>
  </section>

  <section class="flex min-h-screen items-center justify-center bg-cream">
    <p class="text-ink text-headline-3">Section 6 — Cream</p>
  </section>
</Layout>
