---
import Layout from '../layouts/Layout.astro';

// /blog/index.astro
// import { sanityClient } from "sanity:client";
// const posts = await sanityClient.fetch(`*[_type == "post" && defined(slug)] | order(publishedAt desc)`);
---


<Layout>
	<section class="flex min-h-screen items-center justify-center bg-ink">
		<h1 class="font-sans text-white text-heading">Institució Branding</h1>
	</section>

	<section class="flex min-h-screen items-center justify-center bg-navy">
		<p class="text-white text-subhead">Section 2 — Navy</p>
	</section>

	<section class="flex min-h-screen items-center justify-center bg-blue">
		<p class="text-white text-subhead">Section 3 — Blue</p>
	</section>

	<section class="flex min-h-screen items-center justify-center bg-sky">
		<p class="text-white text-subhead">Section 4 — Sky</p>
	</section>

	<section class="flex min-h-screen items-center justify-center bg-mist">
		<p class="text-ink text-subhead">Section 5 — Mist</p>
	</section>

	<section class="flex min-h-screen items-center justify-center bg-cream">
		<p class="text-ink text-subhead">Section 6 — Cream</p>
	</section>
</Layout>
